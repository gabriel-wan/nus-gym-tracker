# Secrets — the Telegram bot token lives in Wrangler secrets, never in the repo
.env
.dev.vars

# Python (prototype/)
__pycache__/
*.pyc
.venv/
venv/

# Node / Cloudflare (from Stage 1 onwards)
node_modules/
.wrangler/

# Local scraper output
*.csv

# Logo/avatar: used once for the Telegram bot, not part of the code
assets/
